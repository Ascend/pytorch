diff --git a/test/test_python_dispatch.py b/test/test_python_dispatch.py
index 041450e066f..471b1b73db0 100644
--- a/test/test_python_dispatch.py
+++ b/test/test_python_dispatch.py
@@ -53,6 +53,7 @@ from torch.utils._python_dispatch import (
)
from torch.utils._pytree import tree_map, tree_map_only

+from torch_npu.contrib import transfer_to_npu

# used as DataLoader collate_fn below; named here to avoid trying to pickle a lambda
def _identity(x):
@@ -2256,6 +2257,7 @@ class TestTensor(torch.Tensor):
                    from torch.fx.experimental.symbolic_shapes import (
                        DimDynamic,
                        ShapeEnv,
                    )
+                    tensor = torch.ones(123).npu()

                    shape_env = ShapeEnv()
                    si = shape_env.create_symintnode(
@@ -2264,12 +2266,12 @@ class TestTensor(torch.Tensor):
                            123,
                            source=ConstantSource("abc"),
                            dynamic_dim=DimDynamic.DUCK,
                            constraint_dim=None,
                        ),
-                        hint=123,
+                        hint=tensor.size(0),
                    )
                    return (si,)

        t = TestTensor()

# NPU Patch Explanation:
# - Added import from torch_npu.contrib import transfer_to_npu
# - Added tensor = torch.ones(123).npu() to provide NPU tensor for size reference
# - Changed hardcoded 123 to tensor.size(0) for dynamic size handling on NPU