diff --git a/test/fx/test_subgraph_rewriter.py b/test/fx/test_subgraph_rewriter.py
index f12f65f..36eafc6 100644
--- a/test/fx/test_subgraph_rewriter.py
+++ b/test/fx/test_subgraph_rewriter.py
@@ -1,3 +1,4 @@
+import torch_npu
 # Owner(s): ["module: fx"]
 
 import os
@@ -49,7 +50,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         # Replace `pattern` with the same pattern (shouldn't change
         # the underlying logic)
@@ -80,7 +81,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -109,7 +110,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(1, 5)
+        x = torch.randn(1, 5).npu()
 
         matches = subgraph_rewriter.replace_pattern_with_filters(traced, pattern, replacement, [])
 
@@ -140,7 +141,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -171,9 +172,9 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
-        w1 = torch.rand(1, 3)
-        w2 = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
+        w1 = torch.rand(1, 3).npu()
+        w2 = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -197,8 +198,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
-        y = torch.randn(4, 5)
+        x = torch.randn(3, 4).npu()
+        y = torch.randn(4, 5).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, pattern)
 
@@ -227,8 +228,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(4, 4)
-        y = torch.randn(4, 4)
+        x = torch.randn(4, 4).npu()
+        y = torch.randn(4, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -261,7 +262,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced_replacement = symbolic_trace(Replacement())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, traced_pattern, traced_replacement)
 
@@ -288,7 +289,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(replacement)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -317,7 +318,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -414,7 +415,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -466,7 +467,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison = Comparison()
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, Pattern(), Replacement())
 
@@ -526,7 +527,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -559,7 +560,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -592,7 +593,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -628,8 +629,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x1 = torch.randn(3, 4)
-        x2 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
+        x2 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -656,9 +657,9 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x1 = torch.randn(3, 4)
-        x2 = torch.randn(3, 4)
-        x3 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
+        x2 = torch.randn(3, 4).npu()
+        x3 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -691,7 +692,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(replacement)
 
-        x1 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
