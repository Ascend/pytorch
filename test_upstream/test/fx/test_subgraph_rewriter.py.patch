diff --git a/test/fx/test_subgraph_rewriter.py b/test/fx/test_subgraph_rewriter.py
index d19ee70..5149c63 100644
--- a/test/fx/test_subgraph_rewriter.py
+++ b/test/fx/test_subgraph_rewriter.py
@@ -55,7 +55,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         # Replace `pattern` with the same pattern (shouldn't change
         # the underlying logic)
@@ -86,7 +86,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -115,7 +115,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(1, 5)
+        x = torch.randn(1, 5).npu()
 
         matches = subgraph_rewriter.replace_pattern_with_filters(
             traced, pattern, replacement, []
@@ -148,7 +148,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -179,9 +179,9 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.rand(1, 3)
-        w1 = torch.rand(1, 3)
-        w2 = torch.rand(1, 3)
+        x = torch.rand(1, 3).npu()
+        w1 = torch.rand(1, 3).npu()
+        w2 = torch.rand(1, 3).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -205,8 +205,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
-        y = torch.randn(4, 5)
+        x = torch.randn(3, 4).npu()
+        y = torch.randn(4, 5).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, pattern)
 
@@ -235,8 +235,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(4, 4)
-        y = torch.randn(4, 4)
+        x = torch.randn(4, 4).npu()
+        y = torch.randn(4, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -269,7 +269,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced_replacement = symbolic_trace(Replacement())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, traced_pattern, traced_replacement)
 
@@ -296,7 +296,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(replacement)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -327,7 +327,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -427,7 +427,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -479,7 +479,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison = Comparison()
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, Pattern(), Replacement())
 
@@ -539,7 +539,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -571,7 +571,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -603,7 +603,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x = torch.randn(3, 4)
+        x = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -638,8 +638,8 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(f)
         comparison_fn = symbolic_trace(comparison)
 
-        x1 = torch.randn(3, 4)
-        x2 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
+        x2 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -666,9 +666,9 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(comparison)
 
-        x1 = torch.randn(3, 4)
-        x2 = torch.randn(3, 4)
-        x3 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
+        x2 = torch.randn(3, 4).npu()
+        x3 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
@@ -748,7 +748,7 @@ class TestSubgraphRewriter(JitTestCase):
         traced = symbolic_trace(M())
         comparison_fn = symbolic_trace(replacement)
 
-        x1 = torch.randn(3, 4)
+        x1 = torch.randn(3, 4).npu()
 
         subgraph_rewriter.replace_pattern(traced, pattern, replacement)
 
