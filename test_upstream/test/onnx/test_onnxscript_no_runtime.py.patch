diff --git a/test/onnx/test_onnxscript_no_runtime.py b/test/onnx/test_onnxscript_no_runtime.py
index e47c88b4c44..4c82c1a17af 100644
--- a/test/onnx/test_onnxscript_no_runtime.py
+++ b/test/onnx/test_onnxscript_no_runtime.py
@@ -1,8 +1,13 @@
 # Owner(s): ["module: onnx"]
 
 """Test the support on onnxscript in PyTorch-ONNX converter."""
+    common_utils.run_tests()
+if __name__ == "__main__":
+
+
 
 import io
+from typing import Sequence
 
 import onnx
 
@@ -10,6 +15,15 @@ import onnxscript
 from onnxscript.onnx_types import FLOAT
 
 import torch
+
+CUDA_VERSION = 11080
+torch.version.cuda = '11.8'
+torch._C._cuda_getCompiledVersion = lambda:11080
+torch._C._cuda_setDevice = torch_npu._C._npu_setDevice
+torch.cuda.get_device_capability = lambda *args, **kwargs:(10,0)
+torch._C._cuda_setStream = torch_npu._C._npu_setStream
+# from torch_npu.contrib import transfer_to_npu
+import torch_npu
 from torch.onnx._internal.torchscript_exporter import jit_utils
 from torch.testing._internal import common_utils
 
@@ -50,7 +64,7 @@ class TestONNXScriptExport(common_utils.TestCase):
         # 2. Register layer_norm onnxscript function as custom Op
         @onnxscript.script(custom_opset)
         def layer_norm(
-            X, axes: list[int], weight: FLOAT[...], bias: FLOAT[...], eps: float
+            X, axes: Sequence[int], weight: FLOAT[...], bias: FLOAT[...], eps: float
         ):
             mean = op.ReduceMean(X, axes=axes)
             D = X - mean  # op.Sub(X, mean)
