diff --git a/test/export/test_converter.py b/test/export/test_converter.py
index 3db578c4511..d2ed0bffb4c 100644
--- a/test/export/test_converter.py
+++ b/test/export/test_converter.py
@@ -5,6 +5,8 @@ from collections import OrderedDict
 from typing import Any, Optional
 
 import torch
+import torch_npu
+import torch_npu.testing
 import torch.utils._pytree as pytree
 from torch._dynamo.test_case import TestCase
 from torch._export.converter import TS2EPConverter
@@ -17,49 +19,55 @@ from torch.testing._internal.torchbind_impls import (
 )
 
 
-requires_cuda = unittest.skipUnless(torch.cuda.is_available(), "requires cuda")
+requires_npu = unittest.skipUnless(torch.npu.is_available(), "requires npu")
 
 
 class TestConverter(TestCase):
     def setUp(self):
-        init_torchbind_implementations()
+        self._torchbind_available = False
+        try:
+            init_torchbind_implementations()
 
-        @torch._library.register_fake_class("_TorchScriptTesting::_TensorQueue")
-        class _FakeTensorQueue:
-            def __init__(self, queue):
-                self.queue = queue
+            @torch._library.register_fake_class("_TorchScriptTesting::_TensorQueue")
+            class _FakeTensorQueue:
+                def __init__(self, queue):
+                    self.queue = queue
 
-            @classmethod
-            def __obj_unflatten__(cls, flattened_ctx):
-                return cls(**dict(flattened_ctx))
+                @classmethod
+                def __obj_unflatten__(cls, flattened_ctx):
+                    return cls(**dict(flattened_ctx))
 
-            def push(self, x):
-                self.queue.append(x)
+                def push(self, x):
+                    self.queue.append(x)
 
-            def pop(self):
-                if self.is_empty():
-                    return torch.empty([])
-                return self.queue.pop(0)
+                def pop(self):
+                    if self.is_empty():
+                        return torch.empty([])
+                    return self.queue.pop(0)
 
-            def size(self):
-                return len(self.queue)
+                def size(self):
+                    return len(self.queue)
 
-            def is_empty(self):
-                return len(self.queue) == 0
+                def is_empty(self):
+                    return len(self.queue) == 0
 
-            def float_size(self):
-                return float(len(self.queue))
+                def float_size(self):
+                    return float(len(self.queue))
 
-        self.torch_bind_ops = [
-            torch.ops._TorchScriptTesting.queue_pop,
-            torch.ops._TorchScriptTesting.queue_push,
-            torch.ops._TorchScriptTesting.queue_size,
-        ]
+            self.torch_bind_ops = [
+                torch.ops._TorchScriptTesting.queue_pop,
+                torch.ops._TorchScriptTesting.queue_push,
+                torch.ops._TorchScriptTesting.queue_size,
+            ]
+            self._torchbind_available = True
+        except (OSError, RuntimeError):
+            self.torch_bind_ops = []
 
     def tearDown(self):
-        torch._library.fake_class_registry.deregister_fake_class(
-            "_TorchScriptTesting::_TensorQueue"
-        )
+        if self._torchbind_available:
+            torch._library.fake_class_registry.deregister_fake_class(
+                "_TorchScriptTesting::_TensorQueue"
+            )
 
     def _check_equal_ts_ep_converter(
         self,
@@ -404,14 +412,14 @@ class TestConverter(TestCase):
         inp = (torch.rand(3, 4),)
         self._check_equal_ts_ep_converter(Module(), inp)
 
-    @requires_cuda
+    @requires_npu
     def test_prim_device_cuda(self):
         class Module(torch.nn.Module):
             def forward(self, x):
                 device = x.device
                 return torch.ones(2, 3, device=device)
 
-        inp = (torch.rand((3, 4), device="cuda:0"),)
+        inp = (torch.rand((3, 4), device="npu:0"),)
         self._check_equal_ts_ep_converter(Module(), inp)
 
     def test_prim_dtype(self):
