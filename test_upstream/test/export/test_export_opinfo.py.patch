diff --git a/test/export/test_export_opinfo.py b/test/export/test_export_opinfo.py
index b33aeb45438..942b5cbe107 100644
--- a/test/export/test_export_opinfo.py
+++ b/test/export/test_export_opinfo.py
@@ -1,6 +1,25 @@
 # Owner(s): ["oncall: export"]
 # ruff: noqa: F841
 # flake8: noqa
+npu_calls_behavior_unchanged()
+    assert npu_y.device.type == "npu"
+    npu_y = npu_x + npu_x
+    npu_x.fake_device = torch.device("npu")
+    npu_x = mode.from_tensor(cpu_x)
+npu_calls_behavior_unchanged()
+    assert torch.npu.device_count() == 0
+    assert torch.npu.is_available() == False
+        torch.npu.current_device()
+        torch.npu.set_device(1)
+        torch.npu.get_device_capability()
+        torch.randn(2, device="npu")
+        npu_x = cpu_x.to("npu")
+def npu_calls_behavior_unchanged():
+    # @unittest.skipIf(not torch.backends.cuda.is_built(), "requires CUDA build")
+    target_device = "npu:0"
+    @onlyPRIVATEUSE1
+    # We set CUDA_VISIBLE_DEVICES="" to simulate a CPU machine with npu build
+    # In CI, this test runs on a CUDA machine with npu build
 
 import itertools
 import subprocess
@@ -14,7 +33,7 @@ from torch.testing._internal.common_device_type import (
     ops,
 )
 from torch.testing._internal.common_methods_invocations import (
-    onlyCUDA,
+    onlyPRIVATEUSE1,
     op_db,
     skip,
     skipOps,
@@ -45,7 +64,7 @@ export_failures = {
     xfail("tensor_split"),
 }
 
-# following are failing fake export on cuda device
+# following are failing fake export on npu device
 fake_export_failures = {
     xfail("geqrf"),
     xfail("histogram"),
@@ -84,7 +103,7 @@ def _test_export_helper(self, dtype, op):
     sample_inputs_itr = op.sample_inputs("cpu", dtype, requires_grad=False)
 
     mode = FakeTensorMode(allow_non_fake_inputs=True)
-    target_device = "cuda:0"
+    target_device = "npu:0"
 
     def to_fake_device(x):
         return x.to(target_device)
@@ -224,6 +243,7 @@ for op in ops:
         self.assertEqual(r, "")
 
     @unittest.skipIf(not torch.backends.cuda.is_built(), "requires CUDA build")
+instantiate_device_type_tests(TestExportOnFakeCuda, globals(), only_for="npu")
     @unittest.skipIf(
         IS_WINDOWS,
         "Failing on Windows, device_count() changes from 0 to 1 ",
