diff --git a/test/distributed/test_backends.py b/test/distributed/test_backends.py
index baf78bb..9fc90a5 100644
--- a/test/distributed/test_backends.py
+++ b/test/distributed/test_backends.py
@@ -1,3 +1,8 @@
+import torch_npu.testing
+
+import torch_npu
+from torch_npu.contrib import transfer_to_npu
+
 # Owner(s): ["oncall: distributed"]
 
 import os
@@ -23,6 +28,8 @@ class TestMiscCollectiveUtils(TestCase):
             assert dist.get_default_backend_for_device(device) == "gloo"
         elif "hpu" in device:
             assert dist.get_default_backend_for_device(device) == "hccl"
+        elif "npu" in device:
+            assert dist.get_default_backend_for_device(device) == "hccl"
         else:
             with self.assertRaises(ValueError):
                 dist.get_default_backend_for_device(device)
@@ -40,11 +47,20 @@ class TestMiscCollectiveUtils(TestCase):
         )
         pg = dist.distributed_c10d._get_default_group()
         backend_pg = pg._get_backend_name()
-        assert backend_pg == backend
+
+        if "npu" in device:
+            # NPU registers via PrivateUse1 mechanism, so _get_backend_name() returns
+            # "custom" rather than the logical name "hccl"
+            assert backend_pg in (backend, "custom"), (
+                f"Expected backend_pg to be '{backend}' or 'custom', got '{backend_pg}'"
+                )
+        else:
+            assert backend_pg == backend
+        
         dist.destroy_process_group()
 
 
-devices = ["cpu", "cuda", "hpu"]
+devices = ["cpu", "cuda", "hpu", "npu"]
 instantiate_device_type_tests(TestMiscCollectiveUtils, globals(), only_for=devices)
 
 if __name__ == "__main__":