diff --git a/test/distributed/test_store.py b/test/distributed/test_store.py
index 310e41f5829..d53e57a1a51 100644
--- a/test/distributed/test_store.py
+++ b/test/distributed/test_store.py
@@ -16,9 +16,11 @@ import torch
 import torch.distributed as dist
 import torch.distributed.distributed_c10d as c10d
 import torch.distributed.rpc as rpc
+import torch_npu
 from torch.distributed import DistError, DistNetworkError, DistStoreError
 from torch.testing._internal.common_distributed import MultiThreadedTestCase
 from torch.testing._internal.common_utils import instantiate_parametrized_tests
+from torch_npu.distributed.rpc.options import NPUTensorPipeRpcBackendOptions
 
 
 if not dist.is_available():
@@ -36,6 +38,7 @@ from torch.testing._internal.common_utils import (
     CONNECT_TIMEOUT,
     load_tests,
     retry_on_connect_failures,
+    skip_but_pass_in_sandcastle,
     run_tests,
     TestCase,
 )
@@ -284,11 +287,12 @@ class FileStoreTest(TestCase, StoreTestBase):
     def test_init_pg_and_rpc_with_same_file(self):
         with tempfile.NamedTemporaryFile(delete=False) as file:
             # Init RPC using file
-            rpc_backend_options = rpc.TensorPipeRpcBackendOptions()
+            torch.npu.set_device(0)
+            rpc_backend_options = NPUTensorPipeRpcBackendOptions()
             rpc_backend_options.init_method = f"file://{file.name}"
             rpc_backend_options._transports = tp_transports()
             rpc.init_rpc(
-                "worker", rank=0, world_size=1, rpc_backend_options=rpc_backend_options
+                "worker", rank=0, world_size=1, backend=rpc.backend_registry.BackendType.NPU_TENSORPIPE, rpc_backend_options=rpc_backend_options
             )
 
             # Init PG using file
@@ -414,7 +418,7 @@ class TCPStoreTest(TestCase, StoreTestBase):
         self.assertRegex(
             repr(store1),
             r"TCPStore\("
-            r"client=TCPClient\(SocketImpl\(fd=\d+, addr=\[?localhost\]?:\d+, remote=\[?localhost\]?:\d+\)\), "
+            r"client=TCPClient\(SocketImpl\(fd=\d+, addr=\[?(?:localhost|::ffff:127\.0\.0\.1)\]?:\d+, remote=\[?(?:localhost|::ffff:127\.0\.0\.1)\]?:\d+\)\), "
             r"server=TCPServer\(port=\d+\)\)",
         )
 
@@ -428,7 +432,7 @@ class TCPStoreTest(TestCase, StoreTestBase):
         self.assertRegex(
             repr(store2),
             r"TCPStore\("
-            r"client=TCPClient\(SocketImpl\(fd=\d+, addr=\[?localhost\]?:\d+, remote=\[?localhost\]?:\d+\)\), "
+            r"client=TCPClient\(SocketImpl\(fd=\d+, addr=\[?(?:localhost|::ffff:127\.0\.0\.1)\]?:\d+, remote=\[?(?:localhost|::ffff:127\.0\.0\.1)\]?:\d+\)\), "
             r"server=<nullptr>\)",
         )
 
@@ -452,13 +456,15 @@ class TCPStoreTest(TestCase, StoreTestBase):
             world_size=1,
         )
 
-        backend_opts = rpc.TensorPipeRpcBackendOptions(
+        torch.npu.set_device(0)
+        backend_opts = NPUTensorPipeRpcBackendOptions(
             init_method=f"tcp://{addr}:{port}", _transports=tp_transports()
         )
         rpc.init_rpc(
             name="worker0",
             rank=0,
             world_size=1,
+            backend=rpc.backend_registry.BackendType.NPU_TENSORPIPE,
             rpc_backend_options=backend_opts,
         )
 
@@ -979,6 +985,7 @@ class TestPythonStore(TestCase):
         self.assertEqual(store.multi_get(["a", "b", "foo"]), [b"c", b"d", b"potato"])
 
 
+@skip_but_pass_in_sandcastle("torch_npu patch is incompatible with ThreadedPG used in MultiThreadedTestCase")
 class TestMultiThreadedWait(MultiThreadedTestCase):
     file_store = dist.FileStore(tempfile.NamedTemporaryFile(delete=False).name, 1)  # noqa: SIM115
     hash_store = dist.HashStore()
