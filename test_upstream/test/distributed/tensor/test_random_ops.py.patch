diff --git a/test/distributed/tensor/test_random_ops.py b/test/distributed/tensor/test_random_ops.py
index 96d3594..bb616ea 100644
--- a/test/distributed/tensor/test_random_ops.py
+++ b/test/distributed/tensor/test_random_ops.py
@@ -1,3 +1,8 @@
+import torch_npu.testing
+
+import torch_npu
+from torch_npu.contrib import transfer_to_npu
+
 # Copyright (c) Meta Platforms, Inc. and affiliates
 # Owner(s): ["oncall: distributed"]
 
@@ -24,9 +29,9 @@ from torch.testing._internal.distributed._tensor.common_dtensor import (
     DTensorTestBase,
     skip_if_lt_x_gpu,
     skip_unless_torch_gpu,
-    with_comms,
+    # with_comms,
 )
-
+from torch_npu.testing.common_distributed import with_comms
 
 TYPE_DEVICE = "hpu" if TEST_HPU else "cuda"
 
@@ -168,13 +173,14 @@ class DistTensorRandomInitTest(DTensorTestBase):
             self.assertEqual(model.weight.device, torch.device("meta"))
 
         # actual initialization
-        device = torch.device("cuda", torch.cuda.current_device())
+        device = torch.device("npu", torch.cuda.current_device())
         model.to_empty(device=device)
         model.reset_parameters()
         self.assertTrue(
             random._rng_tracker is not None
             and isinstance(random._rng_tracker, OffsetBasedRNGTracker)
         )
+        torch.npu.set_device(self.rank)
         self.assertEqual(model.weight.device, device)
         assert isinstance(model.weight, DTensor)
 
@@ -219,7 +225,7 @@ class DistTensorRandomInitTest(DTensorTestBase):
             self.assertEqual(model.weight.device, torch.device("meta"))
 
         # actual initialization
-        device = torch.device("cuda", torch.cuda.current_device())
+        device = torch.device("npu", torch.cuda.current_device())
         model.to_empty(device=device)
         model.reset_parameters()
         self.assertTrue(
@@ -229,6 +235,8 @@ class DistTensorRandomInitTest(DTensorTestBase):
         self.assertEqual(model.weight.device, device)
         assert isinstance(model.weight, DTensor)
 
+
+
         # gather all the shards to compare initialization results
         WORLD = torch.distributed.group.WORLD
         assert WORLD is not None
@@ -564,7 +572,8 @@ class DistTensorRandomOpsTest3D(DTensorTestBase):
             self.assertEqual(model.weight.device, torch.device("meta"))
 
         # actual initialization
-        device = torch.device("cuda", torch.cuda.current_device())
+        torch.npu.set_device(self.rank)
+        device = torch.device("npu", torch.cuda.current_device())
         model.to_empty(device=device)
         model.reset_parameters()
         self.assertTrue(
