diff --git a/test/distributed/tensor/test_random_ops.py b/test/distributed/tensor/test_random_ops.py
index 0e3e7c7d53a..6d9503810f5 100644
--- a/test/distributed/tensor/test_random_ops.py
+++ b/test/distributed/tensor/test_random_ops.py
@@ -30,11 +30,13 @@ from torch.testing._internal.distributed._tensor.common_dtensor import (
     DTensorTestBase,
     skip_if_lt_x_gpu,
     skip_unless_torch_gpu,
-    with_comms,
+from torch_npu.testing.common_distributed import with_comms
+    # with_comms,
+from torch_npu.testing.common_distributed import with_comms
+    # with_comms,
 )
 from torch.utils._typing_utils import not_none
 
-
 def get_generator_seed_for_device_type(device_type: str):
     from torch.distributed._local_tensor import (
         get_generator_seed_for_device_type as _get_seed,
@@ -197,6 +199,11 @@ class DistTensorRandomInitTest(DTensorTestBase):
             self.assertEqual(model.weight.device, torch.device("meta"))
             parallelize_module(model, tp_mesh, ColwiseParallel())
             if random._rng_tracker is not None:
+        device = torch.device("npu", torch.cuda.current_device())
+        torch.npu.set_device(self.rank)
+        device = torch.device("npu", torch.cuda.current_device())
+        torch.npu.set_device(self.rank)
+        device = torch.device("npu", torch.cuda.current_device())
                 random._rng_tracker.distribute_region_enabled = True
 
             self.assertEqual(model.weight.device, torch.device("meta"))
@@ -214,6 +221,8 @@ class DistTensorRandomInitTest(DTensorTestBase):
         self.assertEqual(model.weight.device, device)
         if not isinstance(model.weight, DTensor):
             raise AssertionError(
+
+
                 f"Expected model.weight to be DTensor, got {type(model.weight)}"
             )
 
