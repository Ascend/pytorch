diff --git a/test/test_functionalization.py b/test/test_functionalization.py
index 65e74297a53..9af6aaa815c 100644
--- a/test/test_functionalization.py
+++ b/test/test_functionalization.py
@@ -17,6 +17,7 @@ from torch._subclasses.functional_tensor import (
 from torch.fx.experimental.proxy_tensor import make_fx
 from torch.fx.passes.reinplace import reinplace
 from torch.multiprocessing.reductions import StorageWeakRef
+from torch_npu.contrib import transfer_to_npu
 from torch.testing._internal.common_utils import (
     IS_WINDOWS,
     run_tests,
@@ -236,7 +237,7 @@ class TestFunctionalization(TestCase):
             return x.grad
 
         with torch.autograd.detect_anomaly(check_nan=False):
-            logs = self.get_logs(g, torch.ones(16, 64, 128, 128, requires_grad=True))
+            logs = self.get_logs(g, torch.ones(16, 64, 128, 128, requires_grad=True, device='npu'))
        self.assertExpectedInline(
            logs,
            """\

# NPU Patch Explanation:
# - Added import from torch_npu.contrib import transfer_to_npu
# - Changed torch.ones to use device='npu' for NPU support
# - Enables functionalization tests on NPU hardware
