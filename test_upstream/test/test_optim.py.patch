diff --git a/test/test_optim.py b/test/test_optim.py
index 23094907f94..9168e863287 100644
--- a/test/test_optim.py
+++ b/test/test_optim.py
@@ -13,6 +13,8 @@ from optim.test_optim import TestDifferentiableOptimizer  # noqa: F401
 from optim.test_swa_utils import TestSWAUtils  # noqa: F401
 
 import torch
+from torch_npu.contrib import transfer_to_npu
+import torch_npu
 from torch.nn import Parameter
 from torch.optim import Optimizer, SGD
 from torch.optim.lr_scheduler import ReduceLROnPlateau
@@ -143,7 +145,7 @@ class TestOptimRenewed(TestCase):
         * Grads can also be None, empty, or zero-valued, and this should not disrupt training.
     """
 
-    @onlyCPU
+    # @onlyCPU
     @optims(optim_db)
     def test_optim_infos_do_not_specify_global_cliquey_kwargs(
         self, device, dtype, optim_info
@@ -863,6 +865,7 @@ class TestOptimRenewed(TestCase):
                 optimizers.append(optimizer)
 
             self._compare_between(
+    # @onlyCUDA
                 input, models, optimizers, assert_eq_kwargs, assert_step_dtype
             )
 
@@ -967,7 +970,7 @@ class TestOptimRenewed(TestCase):
                     actual = new_p_state[k]
                     self.assertEqual(og_p_state[k], actual, rtol=rtol, atol=atol)
 
-    @onlyCUDA
+    # @onlyCUDA
     @optims(
         [optim for optim in optim_db if "foreach" in optim.supported_impls],
         dtypes=[torch.float64],
@@ -995,7 +998,7 @@ class TestOptimRenewed(TestCase):
             finally:
                 torch.set_default_dtype(old_default_dtype)
 
-    @onlyCUDA
+    # @onlyCUDA
     @largeTensorTest("72GB", "cuda")
     @serialTest()
     @optims(
@@ -1011,7 +1014,7 @@ class TestOptimRenewed(TestCase):
             optimizer = optim_cls(params, foreach=True, **optim_input.kwargs)
             optimizer.step()
 
-    @onlyCUDA
+    # @onlyCUDA
     @optims(
         [optim for optim in optim_db if "foreach" in optim.supported_impls],
         dtypes=[torch.float32],
@@ -1181,7 +1184,7 @@ class TestOptimRenewed(TestCase):
             optimizer = optim_cls(params, fused=True, **optim_input.kwargs)
             optimizer.step()
 
-    @onlyCUDA
+    # @onlyCUDA
     @optims(
         [optim for optim in optim_db if "fused" in optim.supported_impls],
         dtypes=[torch.float32],
@@ -2232,7 +2235,7 @@ class TestOptimRenewed(TestCase):
             res2 = optim_neg_inf.step(closure)
             self.assertEqual(type(res1), type(res2))
 
-    @onlyCUDA
+    # @onlyCUDA
     @optims(
         [
             optim
