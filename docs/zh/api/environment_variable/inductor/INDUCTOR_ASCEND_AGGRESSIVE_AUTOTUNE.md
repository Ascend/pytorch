# INDUCTOR\_ASCEND\_AGGRESSIVE\_AUTOTUNE

## 功能描述

控制是否启用batch profiler，默认值为“0”。

- 默认值为“0”：关闭batch profiler。
- 配置为“1”：开启batch profiler。

## 配置示例

```bash
export INDUCTOR_ASCEND_AGGRESSIVE_AUTOTUNE=1
```

## 使用约束

无

## 支持的型号

<term>Ascend 950DT</term>
